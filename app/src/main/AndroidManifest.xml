<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kittn.P2Projekt">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/java_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/java_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PdfAnsichtActivity" />
        <activity android:name=".WebViewActivity" />
        <activity android:name=".TestfragenActivity" />
        <activity android:name=".FragenBilderActivity" />
        <activity
            android:name=".VideosActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AuswertungActivity" />
        <activity android:name=".VideoPlayerActivity" />
        <activity android:name=".SkripteActivity" />
    </application>

</manifest>